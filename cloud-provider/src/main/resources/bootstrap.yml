
server:
  port: 8085
spring:
  application:
    name: spring-cloud-nacos-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'


